# Dataset Files (Not Included)

Place the following files inside this folder before running the pipeline:

- `holistic_49_keypoints.pkl`
- `gloss_map.csv`

These files are not included in the repository because of size and licensing restrictions.

Update the correct paths in `configs/default.yaml` if needed.
